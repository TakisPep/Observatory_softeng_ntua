okeanos
	user_space: 
		open/close:		
		4.virtqueue_pop =>elem =>fd
		5.device system call (fd)
		
		ioctl:
		4.virtqueue_pop =>elem =>fd,pipis
		5.pipis = elem.in_sg("kati")
		6.ioctl(fd, mode, pipis) 
	
	utopia
		kernel: 2.copy_from_user(from=arg, to="pipis", len)
			3.virtqueue_add(in_sg:pipis)
			7.copy_to_user(from=pipis, to="kati",len)

		user_space: 1.system call(arg)
